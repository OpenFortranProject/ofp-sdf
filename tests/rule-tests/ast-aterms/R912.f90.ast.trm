Program(
  "!! R912 print-stmt\n!    is  PRINT format [, output-item-list ]\n!\n! Not tested here: format and output-item-list.\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ PrintStmt(
            "13"
          , STAR()
          , comma-list(["\"hello\"", FunctionReference(ProcedureDesignator("that_image"), [])])
          , eos("\n")
          )
        , PrintStmt(
            no-label()
          , IntLiteralConstant("5", no-kind())
          , comma-list(
              [ DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
              , DataRef([PartRef("b", no-section-subscripts(), no-image-selector())])
              , DataRef([PartRef("c", no-section-subscripts(), no-image-selector())])
              ]
            )
          , eos("\n")
          )
        , PrintStmt(
            no-label()
          , IntLiteralConstant("10", no-kind())
          , comma-list([IntLiteralConstant("1", no-kind())])
          , eos("\n")
          )
        , PrintStmt(
            no-label()
          , "'(10f8.2)'"
          , comma-list(
              [ DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
              , DataRef([PartRef("b", no-section-subscripts(), no-image-selector())])
              ]
            )
          , eos("\n\n")
          )
        , FormatStmt(
            "5"
          , FormatSpec(
              format-items-first(
                F(
                  IntLiteralConstant("10", no-kind())
                , IntLiteralConstant("8", no-kind())
                , IntLiteralConstant("2", no-kind())
                )
              )
            , no-list()
            )
          , eos("\n")
          )
        , FormatStmt(
            "10"
          , FormatSpec(
              format-items-first(I(no-r(), IntLiteralConstant("5", no-kind()), no-m()))
            , no-list()
            )
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
