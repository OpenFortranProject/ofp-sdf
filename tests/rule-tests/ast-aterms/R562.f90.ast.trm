Program(
  "!! R562 letter-spec\n!    is letter [ – letter ]\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ ImplicitStmt(
            no-label()
          , [ ImplicitSpec(
                IntrinsicType(REAL(no-kind()))
              , [LetterSpec("a", no-letter()), LetterSpec("b", no-letter()), LetterSpec("c", no-letter())]
              )
            ]
          , eos("\n")
          )
        , ImplicitStmt(
            no-label()
          , [ImplicitSpec(IntrinsicType(REAL(no-kind())), [LetterSpec("D", "g")])]
          , eos("\n")
          )
        , ImplicitStmt(
            no-label()
          , [ImplicitSpec(IntrinsicType(INTEGER(no-kind())), [LetterSpec("y", no-letter())])]
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n\n"))
    )
  ]
)
