Program(
  "!! R755 forall-triplet-spec\n!    is index-name = subscript : subscript [ : stride]\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ ForallConstruct(
            ForallConstructStmt(
              "10"
            , no-name()
            , ForallHeader(
                no-type-spec()
              , [ ForallTripletSpec(
                    "ii"
                  , IntLiteralConstant("1", no-kind())
                  , IntLiteralConstant("10", no-kind())
                  , no-forall-step()
                  )
                , ForallTripletSpec(
                    "jj"
                  , IntLiteralConstant("1", no-kind())
                  , IntLiteralConstant("10", no-kind())
                  , no-forall-step()
                  )
                , ForallTripletSpec(
                    "kk"
                  , IntLiteralConstant("4", no-kind())
                  , IntLiteralConstant("8", no-kind())
                  , no-forall-step()
                  )
                ]
              , no-mask-expr()
              )
            , eos("\n")
            )
          , []
          , EndForallStmt("11", no-name(), eos("\n"))
          )
        , ForallConstruct(
            ForallConstructStmt(
              no-label()
            , no-name()
            , ForallHeader(
                no-type-spec()
              , [ ForallTripletSpec(
                    "ii"
                  , IntLiteralConstant("1", no-kind())
                  , DataRef([PartRef("nn", no-section-subscripts(), no-image-selector())])
                  , no-forall-step()
                  )
                ]
              , no-mask-expr()
              )
            , eos("\n")
            )
          , []
          , EndForallStmt(no-label(), no-name(), eos("\n"))
          )
        , ForallConstruct(
            ForallConstructStmt(
              no-label()
            , no-name()
            , ForallHeader(
                no-type-spec()
              , [ ForallTripletSpec(
                    "ii"
                  , IntLiteralConstant("1", no-kind())
                  , DataRef([PartRef("nn", no-section-subscripts(), no-image-selector())])
                  , colon-forall-step(IntLiteralConstant("5", no-kind()))
                  )
                ]
              , no-mask-expr()
              )
            , eos("\n")
            )
          , []
          , EndForallStmt(no-label(), no-name(), eos("\n"))
          )
        , ForallConstruct(
            ForallConstructStmt(
              no-label()
            , no-name()
            , ForallHeader(
                no-type-spec()
              , [ ForallTripletSpec(
                    "ii"
                  , IntLiteralConstant("1", no-kind())
                  , IntLiteralConstant("30", no-kind())
                  , colon-forall-step(DataRef([PartRef("N", no-section-subscripts(), no-image-selector())]))
                  )
                , ForallTripletSpec(
                    "jj"
                  , DataRef([PartRef("K", no-section-subscripts(), no-image-selector())])
                  , IntLiteralConstant("5", no-kind())
                  , colon-forall-step(IntLiteralConstant("3", no-kind()))
                  )
                ]
              , no-mask-expr()
              )
            , eos("\n")
            )
          , []
          , EndForallStmt(no-label(), no-name(), eos("\n\n"))
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
