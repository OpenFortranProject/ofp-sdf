Program(
  "!! R615 type-param-inquiry\n!    is designator % type-param-name\n!\n!  NOTE 6.8\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ AssignmentStmt(
            no-label()
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , DataRef(
              [PartRef("a", no-section-subscripts(), no-image-selector()), PartRef("kind", no-section-subscripts(), no-image-selector())]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , DataRef(
              [PartRef("s", no-section-subscripts(), no-image-selector()), PartRef("len", no-section-subscripts(), no-image-selector())]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , DataRef(
              [ PartRef("b", paren-list([IntLiteralConstant("10", no-kind())]), no-image-selector())
              , PartRef("kind", no-section-subscripts(), no-image-selector())
              ]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , DataRef(
              [PartRef("p", no-section-subscripts(), no-image-selector()), PartRef("dim", no-section-subscripts(), no-image-selector())]
            )
          , eos("\n\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef(
              [PartRef("a", no-section-subscripts(), no-image-selector()), PartRef("kind", no-section-subscripts(), no-image-selector())]
            )
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef(
              [PartRef("s", no-section-subscripts(), no-image-selector()), PartRef("len", no-section-subscripts(), no-image-selector())]
            )
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef(
              [ PartRef("b", paren-list([IntLiteralConstant("10", no-kind())]), no-image-selector())
              , PartRef("kind", no-section-subscripts(), no-image-selector())
              ]
            )
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef(
              [PartRef("p", no-section-subscripts(), no-image-selector()), PartRef("dim", no-section-subscripts(), no-image-selector())]
            )
          , DataRef([PartRef("z", no-section-subscripts(), no-image-selector())])
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
