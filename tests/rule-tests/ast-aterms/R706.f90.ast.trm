Program(
  "!! R706 level-2-expr\n!    is [ [ level-2-expr ] add-op ] add-operand\n!\n!  level-2-expr is an add-op\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , UnaryPlus(IntLiteralConstant("1", no-kind()))
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , Minus(IntLiteralConstant("3", no-kind()), IntLiteralConstant("1", no-kind()))
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , Plus(
              Minus(IntLiteralConstant("3", no-kind()), IntLiteralConstant("1", no-kind()))
            , IntLiteralConstant("2", no-kind())
            )
          , eos("\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
