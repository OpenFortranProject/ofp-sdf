Program(
  "!! R701 primary\n!    is  constant\n!    or  designator\n!    or  array-constructor\n!    or  structure-constructor\n!    or  function-reference\n!    or  type-param-inquiry\n!    or  type-param-name\n!    or  ( expr )\n!\n! Each alternative is tested separately in the corresponding rule tests.\n! primary is tested as an rhs.\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ DerivedTypeDef(
            DerivedTypeStmt(no-label(), no-list(), "atype", no-list(), eos("\n  "))
          , []
          , []
          , [ DataComponentDefStmt(
                no-label()
              , IntrinsicType(INTEGER(no-kind()))
              , no-list()
              , [ComponentDecl("dim", no-list(), no-list(), no-char-length(), no-init())]
              , eos("\n")
              )
            ]
          , no-type-bound-procedure-part()
          , EndTypeStmt(no-label(), no-name(), eos("\n\n"))
          )
        , TypeDeclarationStmt(
            no-label()
          , IntrinsicType(REAL(no-kind()))
          , no-list()
          , [ EntityDecl(
                "x"
              , ArraySpec([ExplicitShape(no-lower-bound(), IntLiteralConstant("2", no-kind()))])
              , no-list()
              , no-char-length()
              , no-init()
              )
            ]
          , eos("\n")
          )
        , TypeDeclarationStmt(
            no-label()
          , derived-type-spec(DerivedTypeSpec("atype", no-list()))
          , no-list()
          , [EntityDecl("z", no-list(), no-list(), no-char-length(), no-init())]
          , eos("\n\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , IntLiteralConstant("3", no-kind())
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("x", no-section-subscripts(), no-image-selector())])
          , ArrayConstructor(
              AcSpec(
                no-type-spec()
              , [IntLiteralConstant("3", no-kind()), IntLiteralConstant("3", no-kind())]
              )
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , FunctionReference(
              ProcedureDesignator("y")
            , [ ActualArgSpec(no-keyword(), IntLiteralConstant("3", no-kind()))
              , ActualArgSpec(no-keyword(), IntLiteralConstant("2", no-kind()))
              , ActualArgSpec(no-keyword(), IntLiteralConstant("1", no-kind()))
              ]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , FunctionReference(
              ProcedureDesignator("func")
            , [ActualArgSpec(no-keyword(), IntLiteralConstant("1", no-kind()))]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , DataRef(
              [PartRef("z", no-section-subscripts(), no-image-selector()), PartRef("dim", no-section-subscripts(), no-image-selector())]
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , Parens(IntLiteralConstant("3", no-kind()))
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n\n"))
    )
  ]
)
