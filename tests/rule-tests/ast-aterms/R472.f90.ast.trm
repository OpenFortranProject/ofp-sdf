Program(
  "!! R472 ac-value\n!    is expr\n!    or ac-implied-do\n\n! Test of expr in ac_value_list\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ AssignmentStmt(
            no-label()
          , DataRef([PartRef("x", no-section-subscripts(), no-image-selector())])
          , ArrayConstructor(
              AcSpec(
                type-spec-colon(REAL(no-kind()))
              , [RealLiteralConstant("3.1", no-kind()), RealLiteralConstant("4.1", no-kind()), RealLiteralConstant("5.9265", no-kind())]
              )
            )
          , eos("\n\n! Test of ac-implied-do\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("A", no-section-subscripts(), no-image-selector())])
          , ArrayConstructor(
              AcSpec(
                no-type-spec()
              , [ AcImpliedDo(
                    [DataRef([PartRef("I", no-section-subscripts(), no-image-selector())])]
                  , AcImpliedDoControl(
                      VarRef("I")
                    , IntLiteralConstant("1", no-kind())
                    , IntLiteralConstant("1075", no-kind())
                    , no-expr()
                    )
                  )
                ]
              )
            )
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("B", no-section-subscripts(), no-image-selector())])
          , ArrayConstructor(
              AcSpec(
                type-spec-colon(
                  DerivedTypeSpec(
                    "TYPE"
                  , paren-list(
                      [TypeParamSpec(no-keyword(), DataRef([PartRef("Z", no-section-subscripts(), no-image-selector())]))]
                    )
                  )
                )
              , [ RealLiteralConstant("3.6", no-kind())
                , AcImpliedDo(
                    [ Div(
                        RealLiteralConstant("3.6", no-kind())
                      , DataRef([PartRef("I", no-section-subscripts(), no-image-selector())])
                      )
                    ]
                  , AcImpliedDoControl(
                      VarRef("I")
                    , IntLiteralConstant("1", no-kind())
                    , DataRef([PartRef("N", no-section-subscripts(), no-image-selector())])
                    , opt-expr(IntLiteralConstant("2", no-kind()))
                    )
                  )
                ]
              )
            )
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
