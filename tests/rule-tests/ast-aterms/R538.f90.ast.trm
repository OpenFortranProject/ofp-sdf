Program(
  "!! R538 data-i-do-object\n!    is array-element\n!    or scalar-structure-component\n!    or data-implied-do\n!\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ DataStmt(
            no-label()
          , DataStmtSet(
              [ DataImpliedDo(
                  [ DataRef(
                      [ PartRef(
                          "truth"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  , DataRef(
                      [ PartRef(
                          "beauty"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  ]
                , DataIDoVariable(VarRef("i"))
                , IntLiteralConstant("1", no-kind())
                , IntLiteralConstant("2", no-kind())
                , opt-expr(IntLiteralConstant("3", no-kind()))
                )
              ]
            , [DataStmtValue(DataStmtRepeat("15"), RealLiteralConstant("0.", no-kind()))]
            )
          , eos("\n")
          )
        , DataStmt(
            no-label()
          , DataStmtSet(
              [ DataImpliedDo(
                  [ DataRef(
                      [ PartRef(
                          "ugly"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  , DataRef(
                      [ PartRef(
                          "lies"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  , DataRef(
                      [ PartRef(
                          "this"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  , DataRef(
                      [ PartRef(
                          "that"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  , DataRef(
                      [ PartRef(
                          "theother"
                        , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                        , no-image-selector()
                        )
                      ]
                    )
                  ]
                , DataIDoVariable(VarRef("i"))
                , IntLiteralConstant("1", no-kind())
                , IntLiteralConstant("2", no-kind())
                , opt-expr(IntLiteralConstant("3", no-kind()))
                )
              ]
            , [DataStmtValue(DataStmtRepeat("15"), RealLiteralConstant("0.", no-kind()))]
            )
          , eos("\n")
          )
        , DataStmt(
            no-label()
          , DataStmtSet(
              [ DataImpliedDo(
                  [ DataImpliedDo(
                      [ DataRef(
                          [ PartRef(
                              "foo"
                            , paren-list(
                                [ DataRef([PartRef("j", no-section-subscripts(), no-image-selector())])
                                , DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])
                                ]
                              )
                            , no-image-selector()
                            )
                          ]
                        )
                      ]
                    , DataIDoVariable(VarRef("i"))
                    , IntLiteralConstant("1", no-kind())
                    , DataRef([PartRef("j", no-section-subscripts(), no-image-selector())])
                    , no-expr()
                    )
                  ]
                , DataIDoVariable(VarRef("j"))
                , IntLiteralConstant("1", no-kind())
                , IntLiteralConstant("5", no-kind())
                , no-expr()
                )
              ]
            , [DataStmtValue(DataStmtRepeat("15"), RealLiteralConstant("0.", no-kind()))]
            )
          , eos("\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
