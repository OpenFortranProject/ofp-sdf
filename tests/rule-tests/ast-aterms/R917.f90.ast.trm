Program(
  "!! R917 output-item\n!    is expr\n!    or io-implied-do\n!\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ WriteStmt(
            no-label()
          , io-control-spec-list(UNIT(STAR()), FMT(STAR()), no-list())
          , [ DataRef([PartRef("truth", no-section-subscripts(), no-image-selector())])
            , DataRef([PartRef("beauty", no-section-subscripts(), no-image-selector())])
            ]
          , eos("\n")
          )
        , WriteStmt(
            no-label()
          , io-control-spec-list(UNIT(STAR()), FMT(STAR()), no-list())
          , [ DataRef([PartRef("or", no-section-subscripts(), no-image-selector())])
            , DataRef([PartRef("ugly", no-section-subscripts(), no-image-selector())])
            , Plus(IntLiteralConstant("1", no-kind()), IntLiteralConstant("3", no-kind()))
            ]
          , eos("\n\n")
          )
        , WriteStmt(
            no-label()
          , io-control-spec-list(UNIT(IntLiteralConstant("1", no-kind())), no-list())
          , [ IoImpliedDo(
                [DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])]
              , IoImpliedDoControl(
                  VarRef("i")
                , IntLiteralConstant("1", no-kind())
                , IntLiteralConstant("2", no-kind())
                , IntLiteralConstant("3", no-kind())
                )
              )
            ]
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
