Program(
  "!! R444 private-components-stmt\n!    is PRIVATE\n!\n! From Note 4.41\n!\n"
, [ Module(
      ModuleStmt(no-label(), "whut", eos("\n\n"))
    , SpecificationPart(
        []
      , []
      , no-implicit-part()
      , [ DerivedTypeDef(
            DerivedTypeStmt(no-label(), no-list(), "POINT", no-list(), eos("\n"))
          , []
          , [PrivateComponentsStmt("13", eos("\n    "))]
          , [ DataComponentDefStmt(
                no-label()
              , IntrinsicType(REAL())
              , no-list()
              , [ ComponentDecl("X", no-list(), no-list(), no-char-length(), no-init())
                , ComponentDecl("Y", no-list(), no-list(), no-char-length(), no-init())
                ]
              , eos("\n")
              )
            ]
          , no-type-bound-procedure-part()
          , EndTypeStmt(no-label(), "POINT", eos("\n\n"))
          )
        , DerivedTypeDef(
            DerivedTypeStmt("14", no-list(), "Whoot", no-list(), eos("\n  "))
          , []
          , [PrivateComponentsStmt(no-label(), eos("\n"))]
          , [ DataComponentDefStmt(
                "15"
              , IntrinsicType(INTEGER())
              , no-list()
              , [ ComponentDecl("i", no-list(), no-list(), no-char-length(), no-init())
                , ComponentDecl("j", no-list(), no-list(), no-char-length(), no-init())
                , ComponentDecl("k", no-list(), no-list(), no-char-length(), no-init())
                ]
              , eos("\n")
              )
            ]
          , no-type-bound-procedure-part()
          , EndTypeStmt("16", no-name(), eos("\n\n"))
          )
        ]
      )
    , ModuleSubprogramPart(ContainsStmt("17", eos("\n\n")), [])
    , EndModuleStmt("18", no-name(), eos("\n"))
    )
  ]
)
