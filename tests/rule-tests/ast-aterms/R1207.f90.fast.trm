Program(
  [ MainProgram(
      no-program-stmt()
    , Scope(
        [ InterfaceBlock(InterfaceStmt(no-label(), "binky"), [], EndInterfaceStmt(no-label(), "binky"){Comment("
!TODO-F08? INTERFACE OPERATOR(.abstract.)
!TODO-F08? END INTERFACE OPERATOR(.abstract.)
")})
        , InterfaceBlock(
            InterfaceStmt(no-label(), OPERATOR(RelOp_LE()))
          , []
          , EndInterfaceStmt(no-label(), OPERATOR(RelOp_LE()))
          )
        , InterfaceBlock(
            InterfaceStmt(no-label(), OPERATOR(MINUS()))
          , []
          , EndInterfaceStmt(no-label(), OPERATOR(MINUS()))
          )
        , InterfaceBlock(InterfaceStmt(no-label(), ASSIGNMENT()), [], EndInterfaceStmt(no-label(), ASSIGNMENT()){Comment("
!TODO-F08? INTERFACE WRITE(UNFORMATTED)
!TODO-F08? END INTERFACE WRITE(UNFORMATTED)
")})
        ]
      , []
      , []
      )
    , EndProgramStmt(no-label(), no-name())
    )
  ]
){Comment("! Testing generic-spec, R1207
")}
