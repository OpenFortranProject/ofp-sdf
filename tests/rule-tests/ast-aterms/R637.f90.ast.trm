Program(
  "!! R637 allocate-coshape-spec\n!    is [ lower-bound-expr : ] upper-bound-expr\n!\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ AllocateStmt(
            no-label()
          , no-type-spec()
          , [ Allocation(
                DataRef([PartRef("A1", no-section-subscripts(), no-image-selector())])
              , no-list()
              , AllocateCoarraySpec([AllocateCoshapeSpec(no-lower-bound(), IntLiteralConstant("3", no-kind()))], no-lower-bound())
              )
            ]
          , no-list()
          , eos("\n")
          )
        , AllocateStmt(
            no-label()
          , no-type-spec()
          , [ Allocation(
                DataRef([PartRef("A2", no-section-subscripts(), no-image-selector())])
              , no-list()
              , AllocateCoarraySpec(
                  [AllocateCoshapeSpec(IntLiteralConstant("2", no-kind()), IntLiteralConstant("3", no-kind()))]
                , no-lower-bound()
                )
              )
            ]
          , no-list()
          , eos("\n")
          )
        , AllocateStmt(
            no-label()
          , no-type-spec()
          , [ Allocation(
                DataRef([PartRef("A3", no-section-subscripts(), no-image-selector())])
              , no-list()
              , AllocateCoarraySpec(
                  [ AllocateCoshapeSpec(no-lower-bound(), IntLiteralConstant("2", no-kind()))
                  , AllocateCoshapeSpec(IntLiteralConstant("3", no-kind()), IntLiteralConstant("4", no-kind()))
                  ]
                , no-lower-bound()
                )
              )
            ]
          , no-list()
          , eos("\n\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
