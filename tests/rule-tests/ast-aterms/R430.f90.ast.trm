Program(
  "!! R430 sequence-stmt\n!    is SEQUENCE\n\n"
, [ MainProgram(
      no-program-stmt()
    , InitialSpecPart([], [])
    , SpecAndExecPart(
        [ DerivedTypeDef(
            DerivedTypeStmt(no-label(), no-list(), "seq_type", no-list(), eos("\n  "))
          , []
          , [SequenceStmt(no-label(), eos("\n"))]
          , []
          , no-type-bound-procedure-part()
          , EndTypeStmt(no-label(), no-name(), eos("\n\n"))
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-name(), eos("\n"))
    )
  ]
)
