Program(
  "!! R705 add-operand\n!    is [ add-operand mult-op ] mult-operand\n!\n!  add-operand is a mult-op\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , Mult(IntLiteralConstant("1", no-kind()), IntLiteralConstant("2", no-kind()))
          , eos("\n")
          )
        , AssignmentStmt(
            no-label()
          , DataRef([PartRef("a", no-section-subscripts(), no-image-selector())])
          , Div(
              Mult(IntLiteralConstant("1", no-kind()), IntLiteralConstant("2", no-kind()))
            , IntLiteralConstant("3", no-kind())
            )
          , eos("\n")
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
