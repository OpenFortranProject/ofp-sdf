Program(
  [ Module(
      ModuleStmt(no-label(), "R508"){Comment("

")}
    , Scope(
        [ TypeDeclarationStmt(
            no-label()
          , INTEGER()
          , [LanguageBinding(no-name())]
          , [Var("i", INTEGER(), no-init())]
          )
        , TypeDeclarationStmt(
            "10"
          , INTEGER()
          , [LanguageBinding(BindingName("\"cint\""))]
          , [Var("j", INTEGER(), no-init())]
          ){Comment("

")}
        ]
      , []
      , []
      )
    , EndModuleStmt(no-label(), no-name())
    )
  ]
){Comment("!! R508 language-binding-spec
! is BIND (C [, NAME = scalar-char-initialization-expr ])

")}
