Program(
  "!! R757 forall-assignment-stmt\n!    is assignment-stmt\n!    or pointer-assignment-stmt\n\n"
, [ MainProgram(
      no-program-stmt()
    , initial-spec-part([], [])
    , SpecAndExecPart(
        [ TypeDeclarationStmt(
            no-label()
          , IntrinsicType(INTEGER(no-kind()))
          , no-list()
          , [ EntityDecl(
                "A"
              , ArraySpec([ExplicitShape(no-lower-bound(), IntLiteralConstant("10", no-kind()))])
              , no-list()
              , no-char-length()
              , no-init()
              )
            ]
          , eos("\n\n")
          )
        , ForallConstruct(
            ForallConstructStmt(
              no-label()
            , no-name()
            , ForallHeader(
                no-type-spec()
              , [ForallTripletSpec(
                   "i"
                 , IntLiteralConstant("1", no-kind())
                 , IntLiteralConstant("10", no-kind())
                 , no-forall-step()
                 )]
              , no-mask-expr()
              )
            , eos("\n  ")
            )
          , [ AssignmentStmt(
                no-label()
              , DataRef(
                  [ PartRef(
                      "A"
                    , paren-list([DataRef([PartRef("i", no-section-subscripts(), no-image-selector())])])
                    , no-image-selector()
                    )
                  ]
                )
              , IntLiteralConstant("13", no-kind())
              , eos("\n")
              )
            ]
          , EndForallStmt(no-label(), no-name(), eos("\n\n"))
          )
        ]
      )
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-program-name(), eos("\n"))
    )
  ]
)
