Program(
  "!! R553 save-stmt\n!    is SAVE [ [ :: ] saved-entity-list ]\n\n"
, [ MainProgram(
      no-program-stmt()
    , InitialSpecPart([], [])
    , SpecAndExecPart([SaveStmt(no-label(), [], eos("\n\n"))])
    , no-subprogram-part()
    , EndProgramStmt(no-label(), no-name(), eos("\n"))
    )
  ]
)
