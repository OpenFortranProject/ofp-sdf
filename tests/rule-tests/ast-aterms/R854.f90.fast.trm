Program(
  [ MainProgram(
      no-program-stmt()
    , Scope([], [ContinueStmt(no-label()), ContinueStmt("99"){Comment("

")}], [])
    , EndProgramStmt(no-label(), no-name())
    )
  ]
){Comment("!! R854 continue-stmt
!    is  CONTINUE

")}
